# AGENTS.md — KALFA Agent Console (native Android)

Call-center agent app for kalfa.me (Hebrew RSVP SaaS): agents supervise AI voice-RSVP calls, take inbound calls, run outbound campaigns, and watch a realtime dashboard.

**This app is a separate product from `kalfa.me/beta` (the Next.js web app).** It talks to that server over the HTTP contract in §"API contract" and reads the same Supabase project through PostgREST/Realtime with the agent's own JWT. It does not share code, a repo, or a deploy pipeline with `beta/`. Where a decision genuinely depends on how the server works, this file says so and names the boundary — it does not restate the server's implementation.

## Hard rules

1. **Hebrew-first RTL.** All user-facing strings in Hebrew. `LayoutDirection.Rtl` everywhere; paddings/margins use `start`/`end` only — never `left`/`right`. Phone numbers, digits, times, and dates render LTR inside RTL text.
2. **Design system: Bento Grid.** Keep the established Bento Grid layout language — rounded cards on a clean canvas, flat design, no gradients. Brand: primary `#6C4CF1`, secondary `#6366F1`.
3. **Supabase is the ONLY backend.** supabase-kt (auth-kt, postgrest-kt, realtime-kt) version 3.x with Ktor 3.x. Package is `io.github.jan.supabase.auth` (not gotrue). No Firebase Auth / Firestore / any Firebase dependency — the single exception is `firebase-messaging` (FCM push only). Secrets never in code: `SUPABASE_URL` / `SUPABASE_ANON_KEY` reach BuildConfig via the secrets-gradle-plugin from the git-ignored `.env` file (see `.env.example`).
4. **Telephony lives behind `CallEngine` / `CallSession` / `AgentPresence`.** Do NOT rename or remove these interfaces — every swap happens behind them.
   - **The Voximplant SDK line is v3** (`com.voximplant:android-sdk-bom` + `-core` + `-calls`, version `3.2.0`), NOT the legacy monolithic v2. This app is Compose + coroutines end-to-end, so v3 (modular, coroutine-friendly, actively maintained) is the right line — the earlier "v2 GA, v3 still Beta" call this file used to make is stale; v3 left beta in 2026-07 and this app has been building against it since.
   - **A real v3 login + call-session layer already exists**, under `telephony/vox/`: `VoxClientManager` (connect/one-time-key login, MAU-safe — never logs in speculatively), `VoxSdkAuthClient` (talks to `POST /api/agents/sdk-auth`), `VoxCallSession` (wraps a real `Call`: mute/hold/DTMF/hangup, state mapping), `VoxAudioController` (device selection), plus `telephony/CallForegroundService` (microphone-type FGS) and `telephony/CallAudioPermissions` (RECORD_AUDIO + POST_NOTIFICATIONS, already requested on the live-calls screen).
   - **`startOutboundCall`/`monitorCall`/`takeoverCall` are still not wired into `CallEngine`.** `SupabaseCallEngineImpl.startOutboundCall` / `.monitorCall` / `.takeoverCall` all `throw UnsupportedOperationException` on purpose (`data/SupabaseImplementations.kt:892-906`) — they used to fabricate a `MockCallSession`, which was the worse, dishonest option, and were changed to fail loudly instead. **Update 2026-08-14 — the *inbound-to-human offer* path is now wired, narrowly, outside those three methods:** `ensureLoggedIn` is called from `ConsoleViewModel.setAgentStatus(READY)` (via `telephony/presence/PresenceActions`, shared with the presence notification's shade actions) and `VoxClientManager.onIncomingCall` is assigned in `DependencyContainer` to `VoxIncomingCallCoordinator::handleIncomingCall`, which answers the incoming SDK call, publishes it via new `CallEngine.attachIncomingSession`/`clearAttachedSession` methods, and starts/stops `CallForegroundService` around the leg — see `docs/android-presence-and-call-ux.md` §3. Not device-verified. **Still genuinely open:** an agent-initiated outbound dial, and monitor/takeover of an AI-in-progress call — those three methods above are unchanged and still throw. See "Known state" below before starting that work.
   - Self-managed Telecom (`ConsoleConnectionService`) is **not built** — no such class exists yet, and the manifest entry for it is commented out with a `TODO(voximplant-phase)`. `MANAGE_OWN_CALLS` / `FOREGROUND_SERVICE_PHONE_CALL` are deliberately NOT declared until it lands (declaring them with nothing behind them is a Play-review risk, not a shortcut).
   - The mock implementations stay in the tree as the offline/unconfigured fallback (DEBUG builds only — `di/DependencyContainer.kt`'s `mockOrFailClosed` refuses them in `release`); they are not deleted.
5. **Architecture:** Kotlin + Jetpack Compose (Material 3), single-activity, sealed classes for call/campaign state, one immutable `UiState` per ViewModel exposed as `StateFlow`. Every screen has a `@Preview` with fake data. DI: `di/DependencyContainer` (manual service locator) — Hilt is still approved but not yet migrated; the fake→real `CallEngine` swap that was meant to trigger it is now the telephony-wiring step above, not yet done. Migrate incrementally; do not rewrite working screens to chase it.
6. **Allowed libraries:** Compose BOM, Hilt, supabase-kt, Ktor client (okhttp engine), kotlinx-serialization, Coil, firebase-messaging, `com.voximplant:android-sdk-bom`/`-core`/`-calls` (v3, **not** the legacy `voximplant-sdk` v2 artifact), accompanist-permissions. Anything else — ask first.
7. **Source of truth, and its limits.** This file is the contract, but it is NOT automatically current: the production schema and the server routes change in the `kalfa.me/beta` repo, not here. Where this file and the live database — or the server's own contract doc, `beta/docs/voice-agent/app-integration-reference.md` — disagree, **the server side wins**, and fix this file in the same change. Never invent an API path or a column name; if you cannot verify one, ask.
8. **Identity:** base package and applicationId are `me.kalfa.agentconsole` — never `com.example` or AI-Studio-generated ids (Google Play rejects them). The leftover `com.example` template tests are gone (renamed to real tests in `f85bb87` — see "Known state" item 10); nothing under `com.example` should reappear.
9. **KSP versioning:** KSP now uses standalone versions (2.3.x line, decoupled from Kotlin). Do not force the legacy `<kotlin>-<ksp>` format.
10. **Functional code first.** No marketing READMEs or comments-as-docs. **Exception, explicitly approved:** the telephony layer (auth flow, Telecom/ConnectionService lifecycle, monitor/takeover media routing) MUST carry comments explaining *why* — it is the one part of this app whose failure modes are invisible in the code and expensive in production. **Scope clarified 2026-08-14:** this rule governs inline code comments (don't pad a function with a comment that just restates the line beneath it) — it does not mean the top-level `README.md` should stay a thin stub. A real, evidence-based README is documentation a reader needs before they can work in this repo at all, which is a different thing from a comment restating obvious code; the rule was being read as covering both, and a genuinely useful README had not been written as a result. Keep the README substantive; keep code comments lean except where §4 requires the why.

## Phase status (update this section when a phase opens or closes)

| Phase | State | Notes |
|---|---|---|
| Data layer (feed / campaigns / RSVP / analysis) | **DONE** | `data/SupabaseImplementations.kt` reads the real console views; realtime on `console_call_feed` + `agent_status`; Broadcast for live captions (`data/LiveTranscript.kt`) |
| Server API routes the app already calls | **LIVE** | `agents/status`, `calls/{id}/agent-command`, `calls/{id}/end`, `events/{id}/outreach-call` (enqueue, with `already_reached` handling), `campaigns/{id}/status` |
| Voximplant v3 SDK — login layer | **BUILT** | `telephony/vox/VoxClientManager` + `VoxSdkAuthClient` + `VoxCallSession` + `VoxAudioController` compile and are unit-tested (`VoxConfigTest`) against `POST /api/agents/sdk-auth`, which is live server-side. `ensureLoggedIn` is called from `ConsoleViewModel.setAgentStatus(READY)` (via `telephony/presence/PresenceActions` since 2026-08-14) and from a push wake (`VoxFirebaseMessagingService`) — see "Push wake-up" below. |
| Voximplant v3 SDK — wired into `CallEngine` | **OPEN** | The concrete next step; see hard rule 4. |
| Monitor / takeover (server) | **BUILT, feature-flagged off** | `POST /api/calls/{id}/monitor` exists, authorizes, and records the leg — gated behind `app_settings.monitor_enabled` (default OFF), returns `503` until the RSVPAgent VoxEngine scenario carries the supervisor-conference handler and that is verified on a live call. Not this repo's work to unblock; see "Known state". |
| Foreground service / call-audio permissions | **`CallForegroundService` now started/stopped for real (2026-08-14)** | `VoxIncomingCallCoordinator` calls `CallForegroundService.start()`/`.stop()` around an incoming call's ringing and connected phases (`docs/android-presence-and-call-ux.md` §3). The monitor/takeover leg this service was originally described for still doesn't exist — that call site is still open, unchanged by this row. |
| Presence heartbeat (`AgentPresence.setStatus` re-send) | **DONE 2026-08-14** | `telephony/presence/PresenceForegroundService` re-sends `setStatus` every 30s while `AgentPresence.shiftActive` is true, fixing the one-shot write this table used to describe (see "Push wake-up" below and `docs/android-presence-and-call-ux.md` §1). Persistent shade/lock-screen notification with READY/NOT_READY/DND actions. Not device-verified — see that doc's "What could not be verified". |
| Incoming-call notification + `onIncomingCall` wiring | **DONE 2026-08-14** | `VoxClientManager.onIncomingCall` is now assigned (`DependencyContainer`, to `VoxIncomingCallCoordinator::handleIncomingCall`) — see "Push wake-up" below and `docs/android-presence-and-call-ux.md` §3. This is narrower than the row below: it wires the *inbound-to-human* offer path only, through two new `CallEngine`/`CallSession` methods (`attachIncomingSession`/`clearAttachedSession`, `answer()`/`decline()`). It does **not** touch `monitorCall`/`takeoverCall`/`startOutboundCall`. |
| Self-managed Telecom (`ConnectionService`) | **Not started** | Class does not exist; manifest entry commented out. Deferred until a real leg exists to hand it. `docs/android-presence-and-call-ux.md` "Decision: `android.telecom` adoption" recommends Jetpack `androidx.core:core-telecom` (`CallsManager`) over hand-writing `ConnectionService` when this phase starts — verified minSdk 23 (below this app's 24), stable 1.0.1, supplies its own internal `ConnectionService` via manifest merger. Not adopted yet; still requires `MANAGE_OWN_CALLS` + a device to verify the 5s `CallControlScope` callback deadline. |
| Push wake-up for a closed app | **App side DONE 2026-08-14; server side already done 2026-08-14; `onIncomingCall` wired later the same day** | `telephony/vox/VoxFirebaseMessagingService` + persisted-token silent login + `POST /api/agents/shift` from `ConsoleViewModel.setAgentStatus(READY)` (now via `telephony/presence/PresenceActions`, shared with the presence notification) — see "Push wake-up" below. A delivered incoming call now has somewhere to go (previous row); still not verified end to end on a real device — see "Known state" and `docs/android-presence-and-call-ux.md`'s verification list. |
| Hilt migration | **OPEN — approved, deferred** | |
| FCM (`google-services.json`, `FirebaseMessagingService`) | **DONE 2026-08-14** | `VoxFirebaseMessagingService` registered in the manifest, forwards to `Client.handlePushNotification`. `onIncomingCall` is now assigned — see the "Incoming-call notification" row above; this row's job (getting the SDK logged in and registered for push) is unchanged. |

## Known state (read before touching telephony — supersedes the 2026-07-21 "Known defects" audit)

**Dated audit sections in this file have a track record of going stale — this one included, eventually.** The 2026-07-21 "Known defects" audit this section replaced was wrong on the SDK version (said v2, code had already moved to v3) and on at least three items it listed as open that were already fixed in code by the time anyone re-read it (`toggleCampaign`, the hardcoded `"default-event"`, the fabricated-phone-number fallback). None of that was a malicious or careless audit — it was accurate the day it was written and nobody re-verified it before relying on it. Treat every dated section below the same way: it is what was true on 2026-08-14, not a promise about today. Before making a decision that depends on a claim here, check it against the actual code (file:line, same as this section tries to cite) rather than trusting the date in the heading.

Ordered by what actually blocks native calling today.

1. **The real blocker is wiring, not access.** Everything Voximplant login needs exists and is reachable: `console_agents.vox_username` is populated for the one agent, `/api/agents/sdk-auth` is live, and the SDK login/call-session classes are written and compile. **Partially resolved 2026-08-14, narrowly:** the *inbound-to-human offer* path is now wired end-to-end in code (`VoxClientManager.onIncomingCall` → `VoxIncomingCallCoordinator` → `CallEngine.attachIncomingSession`, `docs/android-presence-and-call-ux.md` §3) — not device-verified. `CallEngine.monitorCall`/`.takeoverCall`/`.startOutboundCall` are still untouched and still throw (item 7 below, unchanged). This is still the highest-value next PR for those three.
2. **Monitor/takeover cannot go further than the app anyway.** Even once the app side is wired, the route answers `503` (`app_settings.monitor_enabled` is off) until the `RSVPAgent` VoxEngine scenario implements the supervisor-conference topology and it's verified on a live call — server/scenario work, out of this repo's boundary. Build and test the app side against a scenario the server team stands up for that purpose (see `beta/docs/voice-agent/monitor-scenario-topology.md` §5's verification protocol) — never against `RSVPAgent`/`OutCall` directly.
3. **RSVP-form data loss is now confined to the DEBUG demo path, not production.** `SupabaseRsvpRepository.saveRsvpResult` is still an intentionally empty body — RSVP outcomes belong to the ElevenLabs client-tools pipeline, the console must never write them (`data/SupabaseImplementations.kt:603-607`). The screen that used to silently discard a real answer (`InCallScreen`'s "שמור ונתק") is reachable only from `MainActivity`'s `BuildConfig.DEBUG` mock overlay (`MainActivity.kt:116-124`) — that gate is on `BuildConfig.DEBUG`, not on `currentSession` being null. **Update 2026-08-14:** `currentSession` is no longer *always* null in release — a real, answered incoming call now sets it (`docs/android-presence-and-call-ux.md` §3) — but `InCallScreen`'s render is still gated on `BuildConfig.DEBUG` independently, so it still cannot appear from that path in release (see item 7 below for the precise reasoning). Still worth an honest disabled state if that overlay is ever used for a live demo.
4. **Every call is labelled `אורח` with a blank phone — by design, not a bug.** `DbConsoleCall.toDomain()` hardcodes `customerName = "אורח"` and `customerPhone = ""` (`data/SupabaseImplementations.kt:185-186`) because `console_call_feed` is deliberately PII-free at the database level. Same for `EventGuest`/`RsvpResult` fallback names. If a name is ever needed, it has to come from a gated server field (`view_customer_data`), not a client guess.
5. **`toggleCampaign` is real**, not a no-op: `SupabaseCampaignRepository.toggleCampaign` calls `POST /api/campaigns/{id}/status` with `{action: activate|pause}` and refetches on success (`data/SupabaseImplementations.kt:531-558`).
6. **`enqueueOutboundCall` is real and event-scoped** — the old hardcoded `"default-event"` and string-concatenated JSON body are gone; it POSTs a real `eventId`/`guest_id` pair with a serializer and handles the `already_reached` 409 by `code`, not by matching Hebrew text (`data/SupabaseImplementations.kt:826-876`, `ui/viewmodel/ConsoleViewModel.kt:452-481`).
7. **Monitor / takeover / free-form outbound dial are honestly gated in the UI**, not faked: `ConsoleViewModel.monitorCall/takeoverCall/makeOutboundCall` are `notifyNotWired(...)` — a snackbar, nothing else — and the matching buttons are disabled and labelled "בקרוב" (`ui/screens/LiveCallsScreen.kt:394-421`, `ui/screens/DashboardScreen.kt:321`). **Update 2026-08-14 — `currentSession` is no longer *always* null in release:** a real, answered incoming call now sets it via `CallEngine.attachIncomingSession` (`docs/android-presence-and-call-ux.md` §3) — but `MainActivity`'s `InCallScreen` render is still gated on `BuildConfig.DEBUG` independently of `currentSession`'s nullability, and that gate was deliberately left untouched (see that doc's "Why a (minimal) new screen is added here, and `InCallScreen` is not touched"), so the in-call screen still cannot appear from this path in release — only the minimal `IncomingCallScreen` ring surface can, pre-answer. Re-enable each of the three `notifyNotWired` methods above only when its backend + SDK path is verified end-to-end.
8. **"השתק AI" still sends `clear_buffer`**, which flushes the agent's buffer (barge-in) rather than muting it (`ui/viewmodel/ConsoleViewModel.kt:405-407`). Rename the control or change the command — unresolved.
9. **`CampaignsScreen.kt` is still unreachable** — no route in `ui/NavRoutes.kt`, no entry in `ui/ConsoleDestinations.kt` or the `NavHost` (`MainActivity.kt`). Only its `CampaignCard` composable is reused elsewhere. Wire it or delete it.
10. **Test suite has real coverage; the `com.example` template scaffolding is gone.** `f85bb87` renamed the AI Studio scaffold's `ExampleRobolectricTest` (which asserted `app_name == "My Application"` — a leftover that never got updated and always failed) to `AppIdentityTest`, asserting the real value `"KALFA Agent Console"`; the `com.example` package no longer exists anywhere in `app/src`. Current suite: `AppIdentityTest`/`AppIdentityInstrumentedTest`, `ConsoleEventGuestMappingTest`, `FailureMessagesTest`, `MessageCenterTest`, `ManualDialPolicyTest`, `VoxConfigTest` (guards the SDK full-username format — the #1 silent auth failure), `VoxSilentLoginTest`, `VoxWakePushHandlerTest`. CI's `gradle test` step has never had `continue-on-error` since `f85bb87` — a real test failure fails the run (verified 2026-08-14 against a green CI run, see §"Build & CI").
11. **A fresh checkout without `.env` builds into full mock mode** — fabricated Hebrew names, real-looking phone numbers, `https://example.com/record*.mp3` URLs, auth bypassed. CI now handles this correctly (writes real secrets when present, otherwise builds mock with a loud `::warning::`) — see "Build & CI" below. Never demo a local build without checking which mode it's in.
12. **No i18n.** Every string is a Hebrew literal inline in Kotlin; `strings.xml` holds only `app_name`. Acceptable while Hebrew-only — a recorded cost, not a surprise.

## Push wake-up — resolved architecture, and a caveat that matters more than the architecture

**The reason this app exists:** the browser PWA only receives calls while a tab is open and its 60s heartbeat keeps flowing — a closed laptop silently drops out of the routing pool (`agent_status.updated_at` ages past the 90s freshness window server-side; see `beta`'s `AGENT_STATUS_FRESHNESS_MS` / `findRoutableAgents`). A foreground service fixes "stays reachable while the app is running." It does **not** fix "gets woken when the app isn't running" — that needs push.

### Decided 2026-08-14: Voximplant delivers the push itself — no backend work in `beta`

Two candidate architectures were considered. Verified against live Voximplant docs (`references.androidsdk3.*`, `guides.sdk.android-push`), quoted verbatim: *"Call push notifications are sent automatically. Call the `callUser` method in the VoxEngine scenario and a push notification is automatically sent to the app."* Corroborated by a platform-emitted `Call.PushSent` event with a `push_results[]` array in session logs — the platform reporting on its own send.

**So: no `firebase-admin`, no new Supabase table, no new API route, no new secret, no backend send path in `beta`.** A self-hosted FCM sender (a `console_agent_push_tokens` table + `POST /api/agents/push-token` + a `src/lib/push/fcm.ts` send path parallel to the existing web-push stack) was fully specified in an earlier pass of this document and then ruled out for exactly this reason — it would have duplicated infrastructure Voximplant already provides on the one path where the platform is the natural owner. Not reproducing the detail here on purpose; if this decision needs re-deriving, start from the Voximplant guide quote above, not from scratch.

**What beta's existing push stack (`src/lib/push/{web-push,types,browser}.ts`, `push_subscriptions` table) still is:** Web Push (VAPID) for the browser PWA only. It was never a candidate for the native path — the table shape (`endpoint`/`p256dh_key`/`auth_key`) is Browser Push API-specific and has no equivalent for a Voximplant-delivered native push. Nothing about it changes; it just isn't part of this story.

### What IS needed — entirely client-side, in this repo

- **One-time ops action, not code:** upload the Firebase service-account JSON to the Voximplant control panel (Applications → `kalfa-rsvp` → Push Certificates → GOOGLE). Without this, `callUser` sends no push regardless of anything the app does.
- **Scenario side, one line:** `require(Modules.PushService);` added to `ConsoleInbound.voxengine.js` — no change to any `callUser` call site. Server/ops work, out of this repo, but trivial compared to the backend spec that was ruled out.
- **App side (the actual work here):**
  - Standard Firebase client registration: `google-services.json`, the Google Services Gradle plugin — this is the sink for the `firebase-messaging` dependency that has sat unused since the project was scaffolded. Note the plugin isn't just missing, it was **deliberately removed**: commit `337823b` stripped it from the AI Studio export precisely because there was no JSON yet ("google-services plugin (no JSON yet)"). Re-adding it is a real, if small, step — not just dropping a file in.
  - A `FirebaseMessagingService` subclass forwarding `onMessageReceived` into `Client.handlePushNotification(map)`.
  - `registerForPushNotifications(PushConfig(token))` called after a successful SDK login.
  - `VICalls.setIncomingCallListener` (already present in `VoxClientManager` — `onIncomingCall`) is what fires once the platform delivers the woken-up incoming call.
  - **Persisted-token silent login** (`loginWithAccessToken`/`refreshToken`), not the one-time-key flow, for the wake path specifically: a push-woken app has no human present to type a password, and Voximplant's own guide discourages storing one. The one-time-key flow in "Telephony integration spec" §1 is still correct for an interactive, foreground login — this is a second, separate login path needed only for the wake case.

### The caveat that matters more than the architecture: push does not fix the 13.8 incident

**Verified directly** (`beta/src/app/api/voximplant/console/route-inbound/route.ts:171,242` and `src/lib/data/console-calls.ts`'s `findRoutableAgentVoxUsernames`): `ConsoleInbound` only rings usernames the SERVER hands it as `ring_order`, and `ring_order` is built from `findRoutableAgentVoxUsernames()` — the SAME `status='ready'` + fresh-heartbeat gate as everywhere else in this document. An agent with a stale heartbeat is excluded from `ring_order` **before `callUser` is ever attempted** — which is exactly what happened on 2026-08-13 (`status='ready'`, heartbeat 661 minutes stale, every call answered "no agent available"). No `callUser` attempt means **no push would ever have been sent**, push infrastructure notwithstanding.

**Push solves the telephony layer** (a `callUser` attempt reaching a backgrounded or killed app). **It does nothing for the routability layer** (an agent already excluded before `callUser` is tried).

**Update, 2026-08-14 — the server half has since been decided and shipped** (`beta` commit `8af24ab`, deployed). The resolution was NOT to loosen the freshness gate. Instead, `POST /api/voximplant/console/route-inbound-retry` — the one-shot RETRY wave the scenario fires after the primary ring is exhausted — now returns two audiences in order: heartbeat-fresh agents first (awake, answer immediately), then agents whose `console_agent_shift` row is active and fresh, **without** the heartbeat requirement. Ringing that second group is precisely what makes Voximplant push their device: no `callUser`, no push.

What that changes for this repo:

- **The primary ring is unchanged.** A stale-heartbeat agent is still excluded from the first pass, exactly as on 2026-08-13. This adds a second chance after the first pass fails; it does not make that incident impossible, and nobody should describe it that way.
- **The server side now needs nothing further.** What remains is entirely in this repo (FCM registration, `FirebaseMessagingService`, silent login), plus the one-time control-panel upload and the one-line `require(Modules.PushService);` in the scenario — both ops, not app code.
- **This app must call `POST /api/agents/shift` for any of it to work.** It currently does not (see the API-contract section). With no `console_agent_shift` row, the retry wave will never include this agent and no push will ever be sent, no matter how correctly the FCM side is wired. Treat wiring `/api/agents/shift` as part of the push-wake work, not an optional extra.
- **The cold-start budget is real and unmeasured.** The retry wave rings with `RING_RETRY_WINDOW_MS` (15s) — tighter than the primary `RING_PER_AGENT_MS` (20s). FCM delivery, connect, silent login, `onIncomingCall` and answer all have to fit inside it. No documented SLA covers this; it needs a live-device timing test before anyone claims the path works.

### Two more things worth knowing before this gets built

- **MAU is not the constraint it was treated as earlier in this document.** Multiple devices logged in under one credential count as ONE MAU, and a push-triggered reconnect + silent login on the SAME credential is another login on that same credential — it does not multiply per wake-up. The underlying discipline ("don't log in speculatively when there's nothing to do yet") is still good practice for the interactive one-time-key path, but MAU exhaustion specifically is not a realistic risk for the push-wake path.
- **Unmeasured timing risk, flag before trusting this in the primary ring:** a cold wake — FCM delivery → SDK connect → silent login → `onIncomingCall` fires → app answers — has to complete inside `RING_PER_AGENT_MS`, **verified as `20000` (20s)** in `beta/voxfiles/scenarios/src/ConsoleInbound.voxengine.js:84`. Voximplant's docs promise the push is sent "without delay" but give no number for end-to-end wake latency on a real device. This needs a live-device timing test — cold app state, real network, real OEM — before push-wake is trusted to answer in the primary ring rather than being scoped to a retry wave. Also account for Doze/OEM battery managers, which can swallow a push the platform's own `Call.PushSent` log reports as delivered — "the platform says it sent it" and "the device received it in time" are different claims.

### Done 2026-08-14: the app-side work, and what it does NOT yet close

Built: `telephony/vox/VoxFirebaseMessagingService` (registered in the manifest; `onMessageReceived` runs the app-side steps of the documented six-step sequence — login/connect if necessary → `Client.handlePushNotification` → re-register the token — **blocking**, under a 9s internal timeout, because `FirebaseMessagingService` is a `Service`, not a `BroadcastReceiver`: there is no `goAsync()`, and the framework already holds the process up while `onMessageReceived` runs). `VoxClientManager.ensureLoggedIn` now tries `loginWithAccessToken` → `refreshToken`+retry → the original interactive one-time-key flow, in that order, persisting whatever `AuthParams` the SDK returns after each success (`telephony/vox/VoxTokenStore`, plain DataStore Preferences — see its kdoc for why not `androidx.security`: this app's own supabase-kt session manager already stores a strictly more powerful credential, the Supabase refresh token, in plain app-private SharedPreferences). `ConsoleViewModel.setAgentStatus(READY)` now calls `POST /api/agents/shift {active:true}` and triggers the Voximplant login + push-token registration; `logout()` withdraws shift, unregisters the push token, and clears the persisted session.

**Still open, and load-bearing:**
- **Resolved 2026-08-14 (later the same day), narrowly:** `VoxClientManager.onIncomingCall` is now assigned, in `DependencyContainer`, to `VoxIncomingCallCoordinator::handleIncomingCall` — see `docs/android-presence-and-call-ux.md` §3 for the full flow (CallStyle notification, answer/decline, `CallForegroundService` around the leg, `CallEngine.attachIncomingSession`). This closes the specific gap this bullet used to describe (a push-woken app had nowhere to send a delivered call, so it was dropped) for the inbound-to-human path only — it does **not** mean the broader "Voximplant v3 SDK wired into `CallEngine`" phase is done: `monitorCall`/`takeoverCall`/`startOutboundCall` are untouched and still throw. **Not device-verified** — no physical device was available to build this on; notification rendering, lock-screen/FSI behavior, and whether audio is actually audible after `answer()` are all still open per that doc's "What could not be verified". Do not describe push wake-up as answering a call end to end on a real device until that verification happens.
- Also new 2026-08-14: `telephony/presence/PresenceForegroundService` fixes the heartbeat gap described in "The caveat that matters more than the architecture" above — `AgentPresence.setStatus` now gets re-sent every 30s while on shift, not just once. See `docs/android-presence-and-call-ux.md` §1.
- **Confirmed by the platform itself, 2026-08-14 (live sessions, same day):** `registerCurrentPushToken()` was never completing — Voximplant's own logs say `Call.Failed reason = "No push notifications has been sent"` alongside `push_results: []`, i.e. it held no token to push to. Every other candidate was ruled out live (`ensureLoggedIn` demonstrably works — a separate session reached `Call.Ringing`; Supabase writes work; `console_me` exposes `vox_username`; the installed APK carries valid Firebase config). Root cause was the registration failure being silently discarded — `Result` returned but never checked by the caller. Fixed: `VoxClientManager.registerCurrentPushToken` now tags which of two failure domains occurred (local FCM token fetch vs. Voximplant's own SDK call), and the outcome is both surfaced (`AppMessageCenter`) and persisted (`PresenceStateStore.recordPushRegistrationOutcome`, since `PresenceForegroundService` runs with no Activity alive and an in-memory-only record was being lost exactly when it mattered) — the persistent presence notification now shows push-registration failure too, not just presence-sync failure. See `docs/android-presence-and-call-ux.md`'s "Update 2026-08-14 (even later)". Still unverified against the fix itself: no build containing it had reached a device as of these sessions (the device was running `31791889800`/`ccb7bc5`, predating this repo's own `354d531`).
  - **Corrected, same day, later: the swallowed-`Result` bug was a real bug and a real fix. A follow-up claim that a mis-signed debug APK explained the empty `push_results` was written, then RETRACTED the same day — a timeline check (`gcloud services api-keys describe ... --format='value(updateTime)'`) showed the Firebase API key restriction was applied at 12:47:46 UTC, 83 minutes *after* the 11:24:29 UTC session this doc had cited as evidence, and over two hours after the first identical failures at 10:31 UTC. A restriction that did not yet exist cannot have caused an earlier rejection.** The signing/API-key mechanism is real and is a genuine risk **from 12:47:46 UTC onward** (already mitigated prospectively by `b5a11f4`, which stopped CI publishing the debug APK) — but it is not why push registration failed before that time, and root cause for today's actual incident is **open again**. See `docs/android-presence-and-call-ux.md`'s "Update 2026-08-14 (latest)" (the retraction) and the section immediately after it (a code-derived narrowing: whether the failure the owner saw came from the persistent notification or the in-app banner determines whether the exact Hebrew text is diagnostic at all — `PresenceNotificationBuilder` never renders the which-step `detail` string that the in-app banner does). Also unresolved: `VoxFirebaseMessagingService`'s 9s outer wake-timeout races the Voximplant SDK's own byte-verified 10s internal push-token-registration timeout, so a push-wake re-registration that runs long can produce no recorded outcome at all — real, but unconfirmed as this incident's cause either, and not fixed without the still-open live-device timing test.
  - **2026-08-14, later still — three candidate causes ELIMINATED by measurement, so nobody re-runs them.** Each was a plausible story; each is now closed with evidence a later reader can reproduce:
    1. **The API-key restriction is dead as a cause, on stronger evidence than the `updateTime` check above.** `gcloud logging read 'protoPayload.serviceName="apikeys.googleapis.com"' --project=kalfa-rsvp` shows the key's *entire* mutation history: `CreateApiKey` at **07:31:42 UTC** (by Firebase's own service account, request payload `{"projectId": "120185384715"}` — **no restrictions field at all**), then exactly **two** `UpdateKey` calls, at **12:47:45 and 12:47:48 UTC**, both `updateMask: "restrictions.androidKeyRestrictions"`, both by `admin@nm-digitalhub.com`. So the key was not merely *restricted later* — it carried **no Android application restriction whatsoever** at 11:24:29 UTC. This upgrades the retraction from "the timestamps disagree" to "the restriction did not exist."
    2. **The device's APK is NOT missing Firebase config.** Tempting theory, because the whole Firebase project is only hours old (created 07:29–07:31 UTC) and the CI workflow **silently degrades** the debug build when `GOOGLE_SERVICES_JSON_BASE64` is absent (`.github/workflows/*.yml`, the `if [ -n "$GOOGLE_SERVICES_JSON_BASE64" ]` branch — warning only; the *release* build is separately hard-gated on the file, so no release artifact can lack it). The secret was created **10:09:47 UTC** (`gh secret list`), and the build the device is running — run **31791889800**, sha `9487655`, created **10:21:35 UTC** — logs verbatim: `Wrote app/google-services.json from repository secret — FCM push will be configured in this build.` Config present, 63 minutes before the failure.
    3. **Voximplant does hold an FCM push credential, and it is bound.** `npm run voximplant -- push-credentials` → `#9108 provider=GOOGLE`, `application: kalfa-rsvp.kalfarsvp.voximplant.com (11107202)`. The "uploaded but bound to nothing" case that command exists to catch is not what is happening.

    **Caveat on #2:** it proves *that build had config*, not *that build is installed*. "The device is running `31791889800`" is inherited from the bullet above, which self-contradicts on the sha (`31791889800` is `9487655`; `ccb7bc5` is the 09:23 run that FAILED and produced no artifact). If the owner reinstalled anything, re-check #2 before leaning on it.

    **What `push_results: []` actually means — settled against live docs, and it points AWAY from the server.** `guides.troubleshooting.push` (fetched 2026-08-14 via `voximplant.com/api/v2/getDoc`, the `getTree`/`getDoc` recipe in `beta/docs/voximplant/README.md`, because the rendered docs site returns an empty JS shell to fetches) documents `push_results` as carrying **one entry per registered token**, each with its own `result`, and lists as its first failure row: *"no tokens found" — Push token was not registered for this user/device. → Check the application code and ensure that the push token is registered correctly.* So an **empty array is not a failed send — it is no token on file at all**, and the credential is never even exercised. That is decisive for ordering: a wrong-project credential, a missing certificate, or a rejected key would all produce a **populated** array with an error entry (`DEVICE_UNREGISTERED`, `Forbidden`, `SenderId mismatch`), not `[]`.

    **Therefore the live suspect is the one already written down one bullet up:** `registerCurrentPushToken()` failed and, on the build the device is running (10:21:35 UTC, predating `db49786` at 11:47 UTC), that failure was **silently discarded**. Everything observed — `push_results: []`, `"No push notifications has been sent"`, `Call.Failed`, SIP 480 "User offline" — follows from "no token was ever registered" without needing any server-side fault.

    **The five values `vox_register:` can carry — read from the live SDK reference so the string is interpretable the moment it appears** (`references.androidsdk3.android.sdk.core.pushtokenerror`, via `getDoc`): `Cancelled` (max unprocessed requests reached), `ConnectionClosed` (connection to the Voximplant Cloud closed *while processing the registration request*), `InternalError`, `InvalidToken` (*"for example empty string"*), `Timeout` (not completed in time). Two of those are already-suspected conditions rather than mysteries: `Timeout` is the 9s-vs-10s race noted at the end of this bullet, and `ConnectionClosed` pairs with the Android push guide's explicit statement that *"`registerForPushNotifications` may be called in any `ClientState`, however, **the token is registered only after the client logs in**"* — i.e. a call that returns without error still registers nothing if the login is not actually established, which is precisely the state the owner's flipping "לא מחובר"↔"זמין" notification describes.

    **Single action that decides the remaining branch:** install run **31816353447** (`b134ac4`) — the first build whose banner renders `pushFailureStageSuffix`, distinguishing `fcm_token:` (the device never got a token from Google) from `vox_register:` (Voximplant rejected the token we did get). Do NOT write more code before that string is read; the two branches have nothing in common.

    **One thing worth checking only if that string says `vox_register:`** — do not promote it before then, and do not treat it as the cause: the stored credential's `content.sender_id` is `e60399d59d27f5de3c4d8342ae966b005d442596`, forty hex characters. Live docs (`references.httpapi.structure.pushcredentialcontent`) define that field as *"The sender id provided by Google"*, and a Google FCM sender id is the **numeric** project number — `120185384715` for this project. The stored value is not that shape, `credential_bundle` is empty, and the same structure exposes **no** service-account-JSON field for GOOGLE at all (only `sender_id`), which suggests a legacy-style credential rather than the FCM v1 service-account upload that `docs/android-presence-and-call-ux.md` and the "What IS needed" section above both assume. Unverified alternative that must be excluded first: the API may simply redact this field on read. `GetPushCredential` exposes no upload date and returned no `content.file_name`, so settling it means the control panel (Applications → `kalfa-rsvp` → Push Certificates → the GOOGLE entry), not the API.
  - **`MANAGE_OWN_CALLS` is NOT required for this app to hold `USE_FULL_SCREEN_INTENT` — settled from AOSP source, 2026-08-14.** The owner asked directly whether the missing declaration was the reason no permission prompt appears, and the honest answer is no, on three independent grounds. The permission is defined in `frameworks/base/core/res/AndroidManifest.xml` (fetched from `android.googlesource.com`, `refs/heads/main`) as:
    ```xml
    <permission android:name="android.permission.USE_FULL_SCREEN_INTENT"
                android:protectionLevel="normal|appop" />
    ```
    **`normal` means granted at install time, with no dialog** — there is nothing to prompt for and never was, on any Android version. `appop` is the separately-toggleable app-op layered on top in Android 14. The Android 14 behaviour-change page says *"The Google Play Store revokes default `USE_FULL_SCREEN_INTENT` permissions for any apps that don't fit this profile"* — **but that is a Play-side enforcement, and this app is installed directly from the CI artifact, never through Play.** An earlier note in this thread stated the permission is "revoked automatically" for this app; that was read off the Play policy page without checking whether Play is in the distribution path. It is not. Corrected here rather than left to be repeated.

    So on the owner's sideloaded build the permission should be **granted at install**, and `NotificationManager.canUseFullScreenIntent()` should return `true`. That yields a testable prediction worth acting on: if it returns true, `RingCapabilityChecker.fullScreenIntentAllowed` is true, `canRingOnLockedScreen` is true, and the banner the owner saw was **not** the FSI one — it was `NOTIFICATIONS_BLOCKED_TEXT` (the `!canAlert` branch: `POST_NOTIFICATIONS` denied, or the incoming-call channel set to `IMPORTANCE_NONE`). That is a different permission with a completely different remedy, and `POST_NOTIFICATIONS` **is** a runtime permission with a real dialog — which is where the duplicate-launcher bug fixed in `2b0cdc6` becomes relevant.

    **Do not add `MANAGE_OWN_CALLS` to buy the FSI grant.** Beyond being unnecessary above, the manifest already records the deliberate decision not to (lines 20-26): declaring it without a matching `phoneCall` foreground service and a real `ConnectionService` is a Play-review risk, and `ConsoleConnectionService` is still a commented-out TODO. It also would not help — qualification changes what *Play* does to the app-op, not what the platform grants at install. The permission returns together with self-managed Telecom when that phase is actually implemented (`androidx.core:core-telecom`, per "Known state" item on Telecom above), not before, and not for this reason.

    **Independently corroborated, same evening, from a second primary source.** A parallel read of `core/java/android/app/AppOpsManager.java` (same AOSP repo) found `OP_USE_FULL_SCREEN_INTENT`'s own `AppOpInfo.Builder` entry calls `.setPermission(...)` with no `.setDefaultMode(...)` — unlike dozens of neighboring ops that explicitly default to `MODE_IGNORED`/`MODE_ALLOWED`. The framework's own doc comment on the fallback value, `MODE_DEFAULT`: *"the given caller should use its default security check... only be used with appop permissions"* — i.e. an unset appop tied to a `normal` permission defers to that permission's own (unconditional) grant. Nothing in either AOSP file conditions the grant on calling/alarm capability. Also checked `developer.android.com/about/versions/16/behavior-changes-16` (this app's actual `targetSdk`) directly for a newer, broader restriction superseding the Android-14/Play-only one: no mention of full-screen intent anywhere on that page. The mechanism has not moved into the platform between API 34 and this app's API 36.
- The `loginWithAccessToken`/`refreshToken` parameter order (`fullUsername, token, callback`) is inferred from this SDK's own internal consistency (byte-verified via `javap` against every other `Client.login*` overload, all `(String, String, Callback)`) — it is **not** confirmed against a rendered docs page. `voximplant.com/docs`'s search-driven doc pages returned an empty client-side shell to automated fetches during this change (the JS-rendered content never loaded). Re-verify if `loginWithAccessToken`/`refreshToken` ever fail at the SDK boundary with an argument-shaped error.
- The `AuthParams.accessTokenTimeExpired`/`refreshTokenTimeExpired` unit (seconds-duration vs. absolute epoch-seconds) is likewise unconfirmed for Android v3 specifically; `VoxTokenStore`'s `resolveExpiryMs` disambiguates by magnitude (implausible-as-a-duration ⇒ treat as absolute) rather than guessing — see its kdoc.
- The cold-start timing risk two bullets up is **still unmeasured** — this change does not resolve it, it only makes the path exist to measure. Three distinct paths have different budgets: the happy path (stored access token still valid: one `loginWithAccessToken` round trip) most plausibly fits inside `RING_RETRY_WINDOW_MS`; the expired-access-token path (adds a `refreshToken` round trip + a second `loginWithAccessToken`) is tighter; the tokens-gone interactive fallback (adds `requestOneTimeKey` + a round trip to `beta.kalfa.me` + login) adds the most latency and is reached precisely when a device has been idle long enough for both tokens to lapse — plausibly does NOT fit. A live-device test needs to measure all three, not just report one verdict.
- `MobileServices` on `ConnectOptions` (`Google` / `Huawei`, byte-verified in `android-sdk-core` 3.2.0) is the real v3 replacement for the old `ClientConfig.useHmsForPushNotifications` this document used to call unverified — Huawei push is answerable, just not implemented (nothing in this repo targets Huawei devices today).

## Telephony integration spec (for finishing the wiring)

### 1. Authentication — mostly done, verify against this before changing it

The real flow, as implemented in `telephony/vox/VoxClientManager.kt` + `VoxSdkAuthClient.kt`:

```
Client.connect(ConnectOptions(VoxConfig.node))
  → Client.requestOneTimeKey(fullUsername)                     // full username, not short
  → POST https://beta.kalfa.me/api/agents/sdk-auth {one_time_key}  → {hash}
  → Client.loginWithOneTimeKey(fullUsername, hash)
```

- The hash is computed **server-side only** (`beta`'s `src/lib/data/console-sdk-auth.ts`, `computeOneTimeKeyHash`): `MD5(oneTimeKey + "|" + MD5(shortUsername + ":voximplant.com:" + password))`. The Vox password never reaches the APK.
- `VoxConfig.fullUsername` builds `${voxUsername}@kalfa-rsvp.kalfarsvp.voximplant.com` in exactly one place (`telephony/vox/VoxTelephony.kt`) — pinned by `VoxConfigTest`.
- **Node: resolved, `Node.Node1`.** `VoxConfig.node` used to say `Node.Node2`, "confirmed" from the account's Management API endpoint hostname (`api-node2.voximplant.com`) — that reasoning was wrong, not just weak: the Management API (`beta/src/lib/voximplant/core.ts`) is a different service from the SDK's realtime connection, and its base URL isn't even node-specific (`https://api.voximplant.com/platform_api`, generic). The actual evidence is `beta/src/lib/voximplant/core.ts`'s `MEASURED_CONNECTION_NODE`: a real browser one-time-key login **succeeded on `NODE_1` and no other node tried**, 2026-08-12 — an empirical login against this exact account beats a hostname inference. Fixed in code (`VoxTelephony.kt`) to `Node.Node1`, with the corrected provenance in a comment there. **If `Client.connect()` ever fails, re-check this constant first** — a wrong node fails silently at `connect()`, not `login()`, so it presents as a network problem, not an auth one.
- **MAU discipline still applies.** Call `ensureLoggedIn` only when a real leg must be handled or the agent has declared intent to handle one — never speculatively on app launch. **Correction, 2026-08-14:** this used to also say "never on Ready" and "there isn't one yet" for a call site — both wrong even before this same-day change: `ConsoleViewModel.setAgentStatus(READY)` has called `ensureLoggedIn` since the earlier 2026-08-14 push-wake-up work (now via `telephony/presence/PresenceActions`), and "Ready" is exactly the intended call site (see "Push wake-up" → "Two more things worth knowing" for why MAU exhaustion isn't the risk this originally implied). Each login still counts against the 1,000/month quota and can fail with `LoginMauAccessDeniedError` — the discipline is "don't call it with no reason", not "don't call it on Ready".
- `console_me.vox_username` (nullable) is already threaded into the domain layer (`ConsoleMe.voxUsername`, populated in `ConsoleViewModel.loadIdentity`) — read it from there rather than re-fetching.

### 2. Audio & Hardware (v3 SDK)

- Device selection is a **static object**, not obtained via a manager instance: `com.voximplant.android.sdk.core.audio.AudioDeviceManager.audioDevices` / `.selectedAudioDevice` / `.selectAudioDevice(device)` — wrapped already in `telephony/vox/VoxAudioController.kt`. Its `start()`/`stop()` are `@RequiresOptIn` internal SDK API — do not call them; the SDK activates audio automatically with a live call.
- `VICalls.initialize()` must run once before any call activity — done in `VoxClientManager.ensureInitialized()`.

### 3. Android Telecom & Permissions

- Declared today: `RECORD_AUDIO`, `MODIFY_AUDIO_SETTINGS`, `FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_MICROPHONE`, `USE_FULL_SCREEN_INTENT`, `POST_NOTIFICATIONS`, `BLUETOOTH_CONNECT` (`AndroidManifest.xml`). Since 2026-08-14, also `FOREGROUND_SERVICE_SPECIAL_USE` (the presence heartbeat service — see `docs/android-presence-and-call-ux.md` §1); `MANAGE_OWN_CALLS`/`FOREGROUND_SERVICE_PHONE_CALL` remain intentionally absent, unchanged.
- **Deliberately not yet declared:** `MANAGE_OWN_CALLS`, `FOREGROUND_SERVICE_PHONE_CALL`. On Android 14+, a `phoneCall`-typed foreground service additionally requires self-managed Telecom (a registered `PhoneAccount` with `CAPABILITY_SELF_MANAGED` backed by a real `ConnectionService`) or the default-dialer role. `CallForegroundService` uses a `microphone`-typed FGS instead for v1, which only needs `RECORD_AUDIO`. Add the Telecom permissions in the same change that implements `ConsoleConnectionService` — not before, and not separately (an unbacked sensitive permission is a Play-review risk).
- `EnsureCallAudioPermission()` (`telephony/CallAudioPermissions.kt`) is already composed on the live-calls screen (`MainActivity.kt:308`) so the grant is in hand before a leg exists.

### 4. Live Monitor & Takeover (server-verified topology — do not reinvent)

This section previously described a pattern (`Conference.add(EndpointParameters)` taking an `AgentsClient`) that does not compile against the real typings and was corrected on the server side after a live-docs check. The verified topology, 1:1 from Voximplant's official supervisor guide (`beta/docs/voice-agent/monitor-scenario-topology.md`):

- A `Call` receives only **one** audio stream — a new `sendMediaTo` source replaces the previous one. A leg that must hear two sources (guest + AI) needs a mixer: `VoxEngine.createConference()`, **not** `Conference.add()` (which needs a video-conference rule flag and does not accept an `AgentsClient` at all).
- `monitor` = Supervision mode: `guest.sendMediaTo(conf); agent.sendMediaTo(conf); conf.sendMediaTo(human)` — the human leg only receives.
- `takeover` = full Conference mode: all three legs `sendMediaBetween`/`sendMediaTo` the mixer both ways.
- This is **entirely server/VoxEngine-scenario work** (`RSVPAgent.voxengine.js`), gated behind `app_settings.monitor_enabled` and blocked on live-call verification — not something to implement from the app side. The app's job once the server flag is on: call `POST /api/calls/{id}/monitor` `{mode}`, get back `202 {leg_id, request_id}`, watch `human_agent_call_legs`/Realtime for `dialing → ringing → connected`, and answer the incoming SDK call that arrives via `VoxClientManager.onIncomingCall` — wrapping it in a `VoxCallSession` with `connectedState = MONITORED` or `TAKEN_OVER` (both already exist as domain states, see `CallSession`/`CallState`).
- **Open owner decision, not resolved yet:** whether `takeover` should mean a 3-way conference (agent+human+guest, AI never disconnected) or a full swap (AI closed via the existing `close_agent` command, guest left alone with the human). The server doc's default is the 3-way conference; confirm before building UI copy that promises one or the other.

### 5. SmartQueue (ACD v2)

Unverified against this app's current needs — `require(Modules.SmartQueue)` is documented as the modern ACD module for live queues/agent states, but nothing in this repo or the server contract currently depends on it. Treat as background knowledge, not a near-term task.

## Data model (REAL production schema — kalfa-event-magic project; names are fixed)

Realtime tables (postgresChangeFlow; always start collecting BEFORE channel.subscribe()):
- `agent_status(agent_id, status: ready|not_ready|dnd|in_call, updated_at)` — agent updates own row only. `in_call` is server-managed; the app must never write it.
- `console_call_feed(call_attempt_id, event_id, campaign_id, direction, kind, status, handled_by: ai|agent, agent_id, rsvp_digit, finish_reason, call_duration_sec, callback_iso, created_at, updated_at, takeover_claimed_at, takeover_request_id, participation_state)` — trigger-fed from `call_attempts`, deliberately PII-free (no phone/transcript/recording). Live `status` values: `in_progress|completed|no_answer|cancelled|no_response`.
  - **The last three columns are the takeover coordination fields and the app does not read them yet** (`DbConsoleCall` stops at `updated_at`). They exist so two agents cannot claim the same call. Wire them when takeover goes real.
- `call_dispatch_status(dispatch_id, event_id, contact_id, call_attempt_id, status, reason, created_at, updated_at)` — one row per manual dispatch request. Already wired (`SupabaseCallEngineImpl.enqueueOutboundCall` + `fetchTrackedDispatchStatuses`).
- `human_agent_call_legs(id, call_attempt_id, agent_id, request_id, mode, status, vox_sdk_call_id, vox_leg_call_id, device_id, requested_at, connected_at, disconnected_at, failure_code, metadata)` — realtime-published; server creates the `requested` row when `/api/calls/{id}/monitor` is called, the VoxEngine scenario advances it through `dialing → ringing → connected → disconnected|failed|cancelled`. **Not read by the app yet** — this is the row to watch once monitor/takeover wiring starts.

Read-only VIEWS (fetch/poll only — Supabase Realtime does not fire on views):
- `console_campaigns(id, event_id, status: approved|closed, enabled, start_at, close_at, max_contacts)` — billing columns intentionally hidden
- `console_rsvp_results(id, event_id, guest_id, guest_name, attending: boolean, adults, kids, note, created_at)` — note `attending` is a BOOLEAN, not an enum
- `console_campaign_targets(id, event_id, campaign_id, contact_id, status, current_step_index, next_run_at, reached_at, reached_channel, stop_reason, guest_name, phone)` — `phone` is empty unless the agent holds the `view_customer_data` platform permission
- `console_me(user_id, display_name, vox_username, platform_role, platform_rank, permissions)` — the agent's own identity row, and the source of `vox_username` for SDK login
- `console_event_guests(guest_id, event_id, guest_name, dialable, phone, rsvp_status, has_active_campaign, reached_at, callback_scheduled_at, can_start_outreach_call, call_block_reason)` — manual-dial projection. Enable only when `dialable && has_active_campaign && can_start_outreach_call == true`; nullable booleans are fail-closed.

Write ownership (hard rule): the console READS. RSVP outcomes are written by the ElevenLabs client-tools pipeline; campaign state is billing-coupled and changed only via `POST /api/campaigns/{id}/status`; `outreach_state` belongs to the orchestrator. The only direct writes allowed: own `agent_status` row, and `handled_by`/`agent_id` on `console_call_feed` at takeover.

Gate: console access requires membership in `console_agents` **AND** platform staff membership — `is_console_agent()` is `is_staff() AND exists(console_agents…)`, and `console_agents.user_id` is a foreign key to `platform_staff(user_id)` with `ON DELETE CASCADE`. Consequence for the app: an agent removed from staff loses console access instantly and silently. Treat a sudden empty feed plus RLS denials as "access revoked", not as a network fault.

## API contract (the ONLY external HTTP calls; JWT = Supabase session token)

Base `https://beta.kalfa.me`, header `Authorization: Bearer <jwt>`. This list must match `beta/docs/voice-agent/app-integration-reference.md` (the server's own, actively-maintained record of this contract) and `beta/src/lib/validation/agent-console.ts` in meaning — if they disagree, the server side wins (hard rule 7) and both get fixed together. Never invent a path.

- `POST /api/agents/status` `{"status":"ready|not_ready|dnd"}` → `200 {"ok":true,"status":"..."}`. `in_call` is never client-submitted. **Wired.**
- `POST /api/agents/shift` `{"active": true|false}` → `200 {"ok":true,"active":...}` — standing "on shift" intent for auto-connect-on-launch and the off-duty push-wake audience (see "Push wake-up"). **Live server-side since 2026-08-12. Not called by the app yet.**
- `POST /api/events/{eventId}/outreach-call` `{"guest_id":"uuid"}` → `202 {"status":"accepted","dispatch_id":"uuid","event_id":"uuid"}`. A reached contact returns `409 {"code":"already_reached"}` and creates no job. **Wired.**
- `POST /api/agents/sdk-auth` `{"one_time_key":"..."}` → `200 {"hash":"..."}` — server-side Voximplant login hash (§1 above). Username is resolved from the JWT, never sent in the body. `409` means "authorised, but no SDK identity provisioned" — stop, do not retry. **Live server-side; SDK client code written (`VoxSdkAuthClient`), not yet invoked from any call site.**
- `POST /api/calls/{id}/agent-command` — flat body, not nested under `payload`:
  - `{"command":"contextual_update","text":"…"}` → non-interrupting whisper
  - `{"command":"user_message","text":"…"}` → injects a user turn; **interrupts**
  - `{"command":"clear_buffer"}` → one-shot barge-in
  - `{"command":"close_agent"}` → closes the AI leg, call stays up
  - `text` trimmed, non-empty, max 1000. Strict object — an extra field is a 400. `409` when the call is no longer live. Returns `202 {delivered, applied: "pending"|"confirmed"|"rejected", command, request_id}` — `202` is not "done"; for the two text commands `applied` stays `"pending"` forever (ElevenLabs never acks). **Wired.**
- `POST /api/calls/{id}/end` `{}` — ends the whole call (hangs up on the guest), deliberately separate from agent-command so a mistap can't end a live conversation with one enum value. **Wired.**
- `POST /api/calls/{id}/monitor` `{"mode":"monitor"|"takeover"}` → `202 {"attached":true,"leg_id":"…","request_id":"…","mode":"…"}`. Requires `manage_voice`. **Route, auth, and leg-recording are built server-side; returns `503` until `app_settings.monitor_enabled` flips (blocked on the VoxEngine scenario + live verification — see "Telephony integration spec" §4). Not called by the app yet.**
- `POST /api/campaigns/{id}/status` `{"action":"activate"|"pause"}` → `200 {"ok":true,"status":"..."}`. Requires `campaigns.runstate`, not `manage_voice`. Scope is narrow by owner decision: `active→paused` and `paused→active` only; first activation stays on the web. **Wired.**

**Permissions gate the UI, not just the route** — `console_me.permissions` carries `manage_voice` / `campaigns.runstate` / `view_customer_data`; don't render a control the caller's permission set can't back (a 403 that reaches the user is a UI bug, per the server contract's own rule — "never render a control with no backend behind it").

## Domain facts (fixed — do not redesign)

- Telephony platform: Voximplant, application `kalfa-rsvp` (production — the only scenario that dials real guests). **Corrected 2026-08-14:** this line used to also name `kalfatest` as a sandbox app; `beta/docs/voice-agent/monitor-scenario-topology.md` records it as dead ("With kalfatest dead, that edit lands on the only scenario that dials real guests"). Do not build or test against `kalfatest` as if it were a working sandbox — if a test scenario is needed (e.g. for the SDK login/leg-answering work in "Known state" above), it has to be freshly provisioned, not assumed to exist. AI voice brain: ElevenLabs Agents (Hebrew agent) bridged inside VoxEngine.
- The AI already persists results via its own tools (`save_rsvp`, `mark_dnc`, `notify_owner`, `schedule_callback`) — the app READS results; it never writes RSVP outcomes on behalf of the AI.
- RSVP answers: `attending | declined | maybe | callback`, with adult + children counts.
- An AI call is takeover-capable: monitor (listen-only) / takeover (AI dropped, human bridged) — in this codebase these are `CallEngine` interface calls only.

## Hebrew UI conventions

- Dates `DD.MM.YYYY`; times 24h `HH:mm`; phones displayed `05X-XXX-XXXX`.
- Status labels: זמין / לא זמין / נא לא להפריע / בשיחה. Answers: מגיע / לא מגיע / אולי / חזרו אליי.
- Microcopy short and directive; natural Hebrew over anglicisms.

## Build & CI

- Local: create `.env` (see `.env.example`) with real `SUPABASE_URL`/`SUPABASE_ANON_KEY`, or the app builds and runs in full mock mode (fabricated data, no login — see "Known state" item 11).
- `.github/workflows/android-build.yml` writes `.env` from repo secrets `SUPABASE_URL`/`SUPABASE_ANON_KEY` when present (real/configured build); otherwise falls back to `.env.example` with a loud `::warning::` (mock build) — never silently. Unit tests are NOT `continue-on-error` — a real test failure fails the run. Tests and the debug APK always run and are expected to pass on every push; they do not depend on release signing or FCM config.
- **`google-services.json`** (FCM push config, since the 2026-08-14 push-wake-up work) is gitignored — the repo is public and the file names the Firebase project and its API keys, so it is stored as the base64 repo secret `GOOGLE_SERVICES_JSON_BASE64` and decoded to `app/google-services.json` by the "Provide google-services.json" CI step, the same pattern as the release keystore below. `app/build.gradle.kts` applies the `google-services` Gradle plugin **conditionally** on that file's presence: absent → the debug/test build still runs (no FCM, loud `::warning::` + job-summary note); present → normal behavior, including the plugin's own hard failure if the file is malformed. The secret **is set** (2026-08-14), verified by run 31791101868, whose log shows the CI step taking the `present=true` branch — so the `kalfa-debug-apk` artifact is FCM-configured, not the no-FCM fallback. To re-issue it after the Firebase config changes: `cat app/google-services.json | base64 -w0 | gh secret set GOOGLE_SERVICES_JSON_BASE64 --repo nm-digitalhub/KALFA-ELEVENLABS`.
- **⚠ BEFORE the first Play upload: the Firebase API key restriction must gain Play's app-signing SHA-1, or push breaks for every Play-installed build.** On 2026-08-14 the Android API key `10b5313d-d8dd-4f6b-b9f4-36f1ba07cb15` (project `kalfa-rsvp`) was restricted to `me.kalfa.agentconsole` + SHA-1 `e011b737d04d91d3488c991ca96d089117b8734c` — the **upload** key in `my-upload-key.jks`. That is correct for the APKs distributed directly today, and it is what `gplay preflight` asks for. But Play App Signing **re-signs** the app with Google's own key, so a Play-installed build presents a different fingerprint, fails the restriction, and FCM registration stops — silently, in exactly the way the push work spent a day chasing. After the first upload, read the app-signing certificate SHA-1 from Play Console → App integrity → App signing, and ADD it (do not replace ours — direct APKs still need it):
  `gcloud services api-keys update 10b5313d-d8dd-4f6b-b9f4-36f1ba07cb15 --project=kalfa-rsvp --allowed-application=sha1_fingerprint=<UPLOAD_SHA1>,package_name=me.kalfa.agentconsole --allowed-application=sha1_fingerprint=<PLAY_APP_SIGNING_SHA1>,package_name=me.kalfa.agentconsole`
- **An interrupted test run corrupts `app/build/test-results/` and the next run fails with something that looks like a code error but is not.** Symptom: every test task dies with `java.nio.file.NoSuchFileException: app/build/test-results/testDebugUnitTest/binary/in-progress-results-generic.bin`, with no compiler output. Gradle writes that file during a run and finalizes it to `results-generic.bin` at the end; kill the run in between and the stale directory is left in a state the next run refuses. Fix: `rm -rf app/build/test-results/testDebugUnitTest`, then re-run. Nothing else is needed — the directory is generated output, gitignored via `app/.gitignore`, and Gradle recreates it. Two things this is NOT, both checked on 2026-08-14: it is **not** Gradle issue 36601 (that one is a *permissions* bug, affects 9.3.0/9.3.1, fixed in 9.5.0 — this repo's wrapper is 9.6.1 and the generated files verify as `644` under `umask 0022`), and it **cannot reach CI**, which checks out fresh and whose `gradle/actions/setup-gradle` cache covers the Gradle user home, not the project's `build/` directory. Also note that a re-run after the fix can report `BUILD SUCCESSFUL` while the test task is `FROM-CACHE` — that is not proof your test ran; force it with `--rerun-tasks` and read `app/build/test-results/testDebugUnitTest/TEST-*.xml`.
- **The release AAB/APK are only built when BOTH the release keystore secrets AND `google-services.json` are present** — checked in one "Check release readiness" step. `app/build.gradle.kts`'s `release` signing config looks for `KEYSTORE_PATH`/`STORE_PASSWORD`/`KEY_PASSWORD` env vars and falls back to generating a fresh `debug.keystore` when they're absent; Play requires the SAME signing key across every release, so a build signed this way could never be uploaded to Play — CI refuses to produce one rather than let it look uploadable. Missing FCM config is refused for the same reason: a release artifact should never ship with silently-broken push. When either is missing, the release steps are **skipped** (not failed) with a `::warning::` and a job-summary explanation — this must never take down unit tests or the debug APK, which have nothing to do with it. Provisioning one real upload keystore and wiring `RELEASE_KEYSTORE_BASE64`/`RELEASE_KEYSTORE_STORE_PASSWORD`/`RELEASE_KEYSTORE_KEY_PASSWORD` as repo secrets is required before any Play distribution, internal testing track included.
